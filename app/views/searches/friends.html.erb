<% if @friends.size < 1 %>
  <p>You don't have any friends yet.</p>
<% else %>
  <ul>
    <% @friends.each do |friend| %>
      <li><%= "#{friend['firstName']} #{friend['lastName']}" %></li>
    <% end %>
  </ul>
<% end %>
